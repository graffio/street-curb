# Workflow

Process rules live in these places:

| What | Where |
|------|-------|
| Workflow overview | `CLAUDE.md` → Workflow section |
| Planning a feature | `/workflows:plan` command |
| Finishing work | `/workflows:wrap-up` command |
| Task execution | `docs/brainstorms/{name}.task.json` (follow exactly) |
| Quality checks | `review <file>` or `review staged` |
| Commit format | `.claude/tasks/commit-changes.md` |
| Full protocol explanation | `.claude/README.md` |
