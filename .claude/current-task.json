{
  "feature": "Fix recursive type imports in type generator",
  "goal": "Prevent type generator from importing a type that is currently being defined (e.g., FilterSpec importing itself)",
  "templates_used": ["commit-changes.md"],
  "steps": [
    { "step": 1, "action": "In generateStaticTaggedType (line ~210): Filter out self-references from newChildTypes by adding `.filter(typeName => typeName !== name)`", "done": false },
    { "step": 2, "action": "In generateStaticTaggedSumType (line ~337): Same fix - filter out the type being defined from childTypes", "done": false },
    { "step": 3, "action": "Regenerate FilterSpec to verify fix works", "done": false },
    { "step": 4, "action": "Commit with descriptive message", "done": false }
  ],
  "verification": [
    "Generated filter-spec.js does not import FilterSpec",
    "yarn types:generate-all completes without error",
    "No lint errors"
  ]
}
